# Services

This directory contains API client wrappers. Components should never call APIs directly - all external calls go through this layer.

## Planned Services

- `supabase.js` - Supabase client for database operations
- `cbs.js` - CBS Sports API proxy calls (future)
